#include <bits/stdc++.h>
using namespace std;

int main() {
    #ifndef ONLINE_JUDGE
        if (freopen("input.txt", "r", stdin) == NULL) {
            perror("freopen input failed");
            return 1;
        }
        if (freopen("output.txt", "w", stdout) == NULL) {
            perror("freopen output failed");
            return 1;
        }
    #endif

    cout << "Testing output redirection!\n";
    return 0;
}
